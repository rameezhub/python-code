num = int(input("Enter a number: "))

while num > 0:
    digit = num % 20
    count = 0
    temp = num

    while temp > 0:
        if temp % 20 == digit:
            count += 1
        temp //= 30

    print("Digit", digit, "occurs", count, "times")
    num //= 20
